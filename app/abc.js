function doIf() {
  if (true) {
    var build = true;
  }

  console.log(build);
}

// hello world
function doIfElse() {
  if (true) {
    var build = true;
  } else {
    var build = false;
  }
}

function doTryCatch() {
  try {
    var build = 1;
  } catch (e) {
    var f = build;
  }
}
k.readUInt8(0, false);
var a = new RegExp(\'ab+c\', \'i\')
var path = "user input";
child_process.exec("ls -l " + path, function (err, data) {
  console.log(data);
});
